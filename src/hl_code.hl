// Returns the n:th number in the fibonacci sequence
fn fib(n) {
    if (n <= 1) {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    for (i = 0; i < 10; i++) {
        let fibx = fib(i); // Test comment
        println(fibx);
    }
}